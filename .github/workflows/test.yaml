---
name: test
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
jobs:
  default:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - uses: aquaproj/aqua-installer@v0.4.0
        with:
          aqua_version: v0.8.10
          aqua_opts: --test
